# Repository guidance

For Rust changes and code reviews, read [docs/rust-conventions.md](docs/rust-conventions.md).
It defines the shared review baseline, TUI module responsibilities, behavioral
invariants, and validation commands. Apply it to the code being changed; avoid
unrelated repository-wide style rewrites.

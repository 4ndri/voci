#!/usr/bin/env bash
#MISE description="Install or update voci and set up shell completions for the current user"
#MISE raw_args=true

set -euo pipefail

exec "$(dirname "${BASH_SOURCE[0]}")/../scripts/xtask" install "$@"
